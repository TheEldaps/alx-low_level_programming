#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int g = 0;
	int q = 10;

	for (g = 0; g < q; g++)
		printf("%d", g);
	printf("\n");

	return (0);
}
