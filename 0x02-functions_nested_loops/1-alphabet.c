#include "main.h"
/**
 *print_alphabet - is the entry and where the execution starts
 *
 */

void print_alphabet(void)
{
	int i = 'a';
	int zee = 'z';

	for (i = 'a'; i <= zee; i++)
		_putchar(i);
	_putchar('\n');
}
